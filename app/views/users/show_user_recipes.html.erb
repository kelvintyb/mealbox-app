<div class="section" id="my-recipe">
  <h1> My Recipes </h1>
  <span class="headerLine"></span>
  <div class="recipe-all">

    <span class="subtle card-category"><%=link_to "Profile", user_path(current_user.id) %> >  My Recipes </span>

    <br/><br/>


  <div class="masonry-container">
      <div class="grid-sizer"></div>
      <% @recipes.each do |recipe| %>
      <div class="thumbnail recipe-thumb grid-item">
        <figure class="snip1268">
    <div class="image">
      <img src="<%= recipe.image %>" alt="sq-sample4">
  <div class="icons">
        <div class="recipe-views"><span class="glyphicon glyphicon-eye-open views-icon"></span><%= recipe.views %></div>
      </div>
      <%= link_to 'View Recipe', recipe_path(recipe), class:'view-recipe' %>
    </div>
  </figure>


        <div id="ribbon">
      <span id="content-cuisine"><%= recipe.cuisine %></span>
      </div>

        <div class="caption">
          <h3><%= recipe.name %></h3>

          <span class="edit-recipe-btn">
      <i class="fa fa-pencil-square-o"><%= link_to 'Edit Recipe', edit_recipe_path(recipe) %></i>
      </span>

        </div>



      </div>

      <% end %>
  </div>
</div>
</div>
